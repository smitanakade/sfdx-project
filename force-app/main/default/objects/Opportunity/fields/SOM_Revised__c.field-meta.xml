<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>SOM_Revised__c</fullName>
    <externalId>false</externalId>
    <formula>IF( 
OR( 
AND(MONTH(Estimated_Completion_Date__c)=MONTH(TODAY()), 
YEAR(Estimated_Completion_Date__c)=YEAR(TODAY()), 
ISNULL(Completed_Date__c)), 
AND(MONTH(Completed_Date__c)=MONTH(TODAY()), 
YEAR(Completed_Date__c)=YEAR(TODAY()))), 



Attainable_TTV_annual__c, 0)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>SOM Revised</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <type>Currency</type>
</CustomField>
